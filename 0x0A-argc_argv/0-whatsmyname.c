#include "main.h"
#include <stdio.h>

/**
 * main - Prints its name
 * @argv: The argument vector
 * @argc: The argument count
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
